//! Library entry points for the `url_enum` command-line scanner.
//!
//! The crate exposes CLI argument parsing types and the scanner orchestration
//! used by the binary target.

pub mod cli;
pub mod scanner;
