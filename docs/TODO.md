# url_enum TODO

最后更新：2026-06-03

本文档基于当前已实现版本维护后续工作，并记录经复核后无需独立实现的事项。

## 已交付基线

- [x] 使用 `-t/--target` 与 `-d/--dict` 执行路径枚举。
- [x] 使用 `--replace ENUM` 等占位符进行 URL 与 HTTP Header 名称/值模板替换。
- [x] 支持异步受控并发、连接池复用与请求超时。
- [x] 支持发送前请求抖动，用于降低短时请求突发压力。
- [x] 支持 GET/HEAD、User-Agent、重定向与证书校验选项。
- [x] 跟随重定向时保留链上实际 3xx 与落地响应记录，并防止敏感 Header 跨目标转发。
- [x] 支持使用多次 `-H/--header` 按顺序添加自定义 HTTP Header。
- [x] 支持状态码过滤，以及 `--black-size` 按实际响应大小的单值、列表和闭区间排除。
- [x] 支持 HTTP(S) 与 SOCKS5/SOCKS5H 代理，以及代理 URL 内嵌认证。
- [x] 支持扩展名候选生成、CSV/JSONL 输出。
- [x] 防止输出文件覆盖字典输入（含硬链接指向同一文件的情况）。
- [x] 避免追加模式对百分号编码字典路径（包括原始字节）进行重复编码。
- [x] 修正追加 `.`/`..` 时的 URL 规范化语义，避免请求原目标路径。
- [x] 提供单元测试、release 构建和基础端到端验证。
- [x] 在帮助与双语 README 中显著提示仅扫描获得授权的目标。
- [x] 命令行 `--help` 用户提示统一使用英文。
- [x] 添加可重复运行的本地吞吐基准测试，比较并发设置和 CSV/JSONL 输出模式。

## 无需独立实现

- [x] 专用 Cookie 配置参数：使用 `-H 'Cookie: name=value'` 已可发送 Cookie；跨目标重定向时，现有敏感 Header 保护也会停止转发 `Cookie`，无需增加单独选项。

## P0：仍需完成的正确性与安全边界

- [ ] 为模板替换模式明确字典项在 URL 中的编码规则，并覆盖路径、主机名和查询参数位置的测试。
- [x] 添加基于本地测试服务的自动化集成测试，覆盖状态过滤、大小过滤、超时与 HEAD。
- [ ] 明确 `--black-size` 与响应读取失败的处理语义，并为部分响应或连接中断增加测试。

## P1：仍建议完成的运行控制能力

- [ ] 增加每秒请求数限制，便于更精确地控制目标压力。
- [ ] 支持基于状态码或网络错误的有限重试及退避策略。
- [ ] 评估极大字典下的去重内存成本，并提供关闭去重或有界去重策略。
- [ ] 支持 `Ctrl-C` 优雅终止并刷新已产生的输出；如需统计，必须使用显式启用的独立选项。

## P2：可选增强

- [ ] 添加显式启用的进度或统计模式，例如状态码分布、请求速率和错误数量，默认输出仍仅包含结果。
- [ ] 支持保存响应标题或最终重定向 URL 等可选字段。
- [ ] 对具有 `Content-Length` 的 GET 响应优化大小过滤，能够提前排除已知大小结果。
- [x] 添加可重复运行的基准测试，用于比较并发设置和输出模式的吞吐差异。

## 下一步建议顺序

1. 先定义模板替换时的 URL 编码行为，明确大小过滤失败语义，并补集成测试框架。
2. 再实现限速、重试、优雅退出和大字典去重策略，补齐实际扫描控制能力。
3. 最后按需求建设进度输出、附加结果字段和性能优化。
